services:
  postgres:
    image: postgres:16-alpine
    container_name: audiencia-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: audiencia
    volumes:
      - audiencia_data:/var/lib/postgresql/data

volumes:
  audiencia_data:
